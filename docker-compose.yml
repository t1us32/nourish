name: nourish

services:
  api:
    build: ./backend
    environment:
      FDC_API_KEY: ${FDC_API_KEY:-}
    restart: unless-stopped

  web:
    build: ./frontend
    depends_on:
      - api
    ports:
      - "${NOURISH_PORT:-8080}:80"
    restart: unless-stopped
